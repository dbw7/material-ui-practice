import { Typography } from "@mui/material";
import './About.css';

const About = (props) =>{
    return(
        <div className="about">
            <Typography>This is an about page.</Typography>
        </div>
    )
}

export default About;